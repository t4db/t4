# cluster — 3-node T4 cluster (with MinIO) vs 3-node etcd Raft cluster.
#
# T4 model: leader streams WAL entries to followers; all followers ACK
# before the write completes; WAL is archived to MinIO. Leader election is
# via an S3 object lock — no static topology configuration needed.
#
# etcd model: standard Raft consensus, quorum of 2/3 nodes required per write.
#
# The benchmark connects to all three nodes of each cluster and lets the
# etcd v3 client route writes to the current leader automatically.
#
# Ports exposed on the host:
#   t41 → localhost:3379   etcd1 → localhost:2379
#   t42 → localhost:3380   etcd2 → localhost:2380
#   t43 → localhost:3381   etcd3 → localhost:2381
#
# Images must be pre-built before running:
#   docker build -f bench/Dockerfile.t4 -t t4-bench .
#   docker build -f bench/Dockerfile        -t t4bench  .

x-t4-env: &t4-env
  AWS_ACCESS_KEY_ID: bench
  AWS_SECRET_ACCESS_KEY: bench123
  AWS_DEFAULT_REGION: us-east-1

x-t4-base: &t4-base
  image: t4-bench
  environment: *t4-env
  depends_on:
    minio-init:
      condition: service_completed_successfully
  networks: [bench]
  deploy:
    resources:
      limits:
        cpus: '1'
        memory: 2G

x-etcd-base: &etcd-base
  image: gcr.io/etcd-development/etcd:v3.6.4
  networks: [bench]
  deploy:
    resources:
      limits:
        cpus: '1'
        memory: 2G

# Shared etcd cluster environment (node-specific vars added per service).
x-etcd-cluster-env: &etcd-cluster-env
  ETCD_DATA_DIR: /etcd-data
  ETCD_INITIAL_CLUSTER_TOKEN: bench-etcd
  ETCD_INITIAL_CLUSTER_STATE: new
  ETCD_INITIAL_CLUSTER: etcd1=http://etcd1:2380,etcd2=http://etcd2:2380,etcd3=http://etcd3:2380

services:

  # ── MinIO (S3 store for T4 WAL + leader election) ─────────────────────

  minio:
    image: minio/minio:latest
    command: server /data --console-address :9001
    environment:
      MINIO_ROOT_USER: bench
      MINIO_ROOT_PASSWORD: bench123
    volumes:
      - minio-data:/data
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:9000/minio/health/live"]
      interval: 5s
      timeout: 3s
      retries: 10
    networks: [bench]

  minio-init:
    image: minio/mc:latest
    depends_on:
      minio:
        condition: service_healthy
    entrypoint: >
      /bin/sh -c "
        mc alias set local http://minio:9000 bench bench123 &&
        mc mb --ignore-existing local/bench
      "
    networks: [bench]

  # ── T4 cluster ─────────────────────────────────────────────────────────

  t41:
    <<: *t4-base
    hostname: t41
    command:
      - run
      - --node-id=s1
      - --listen=0.0.0.0:2379
      - --peer-listen=0.0.0.0:3380
      - --advertise-peer=t41:3380
      - --data-dir=/data
      - --metrics-addr=0.0.0.0:9090
      - --wal-sync-upload=false
      - --s3-bucket=bench
      - --s3-prefix=cluster
      - --s3-endpoint=http://minio:9000
      - --log-level=warn
    ports:
      - "3379:2379"
    volumes:
      - t41-data:/data
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:9090/healthz"]
      interval: 5s
      timeout: 3s
      retries: 30
      start_period: 15s

  t42:
    <<: *t4-base
    hostname: t42
    command:
      - run
      - --node-id=s2
      - --listen=0.0.0.0:2379
      - --peer-listen=0.0.0.0:3380
      - --advertise-peer=t42:3380
      - --wal-sync-upload=false
      - --data-dir=/data
      - --metrics-addr=0.0.0.0:9090
      - --s3-bucket=bench
      - --s3-prefix=cluster
      - --s3-endpoint=http://minio:9000
      - --log-level=warn
    ports:
      - "3380:2379"
    volumes:
      - t42-data:/data
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:9090/healthz"]
      interval: 5s
      timeout: 3s
      retries: 30
      start_period: 15s

  t43:
    <<: *t4-base
    hostname: t43
    command:
      - run
      - --node-id=s3
      - --listen=0.0.0.0:2379
      - --peer-listen=0.0.0.0:3380
      - --advertise-peer=t43:3380
      - --wal-sync-upload=false
      - --data-dir=/data
      - --metrics-addr=0.0.0.0:9090
      - --s3-bucket=bench
      - --s3-prefix=cluster
      - --s3-endpoint=http://minio:9000
      - --log-level=warn
    ports:
      - "3381:2379"
    volumes:
      - t43-data:/data
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:9090/healthz"]
      interval: 5s
      timeout: 3s
      retries: 30
      start_period: 15s

  # ── etcd cluster ───────────────────────────────────────────────────────────

  etcd1:
    <<: *etcd-base
    hostname: etcd1
    environment:
      <<: *etcd-cluster-env
      ETCD_NAME: etcd1
      ETCD_LISTEN_CLIENT_URLS: http://0.0.0.0:2379
      ETCD_ADVERTISE_CLIENT_URLS: http://etcd1:2379
      ETCD_LISTEN_PEER_URLS: http://0.0.0.0:2380
      ETCD_INITIAL_ADVERTISE_PEER_URLS: http://etcd1:2380
    ports:
      - "2379:2379"
    volumes:
      - etcd1-data:/etcd-data
    healthcheck:
      test: ["CMD", "etcdctl", "--endpoints=http://localhost:2379", "endpoint", "health"]
      interval: 5s
      timeout: 3s
      retries: 30
      start_period: 10s

  etcd2:
    <<: *etcd-base
    hostname: etcd2
    environment:
      <<: *etcd-cluster-env
      ETCD_NAME: etcd2
      ETCD_LISTEN_CLIENT_URLS: http://0.0.0.0:2379
      ETCD_ADVERTISE_CLIENT_URLS: http://etcd2:2379
      ETCD_LISTEN_PEER_URLS: http://0.0.0.0:2380
      ETCD_INITIAL_ADVERTISE_PEER_URLS: http://etcd2:2380
    ports:
      - "2380:2379"
    volumes:
      - etcd2-data:/etcd-data
    healthcheck:
      test: ["CMD", "etcdctl", "--endpoints=http://localhost:2379", "endpoint", "health"]
      interval: 5s
      timeout: 3s
      retries: 30
      start_period: 10s

  etcd3:
    <<: *etcd-base
    hostname: etcd3
    environment:
      <<: *etcd-cluster-env
      ETCD_NAME: etcd3
      ETCD_LISTEN_CLIENT_URLS: http://0.0.0.0:2379
      ETCD_ADVERTISE_CLIENT_URLS: http://etcd3:2379
      ETCD_LISTEN_PEER_URLS: http://0.0.0.0:2380
      ETCD_INITIAL_ADVERTISE_PEER_URLS: http://etcd3:2380
    ports:
      - "2381:2379"
    volumes:
      - etcd3-data:/etcd-data
    healthcheck:
      test: ["CMD", "etcdctl", "--endpoints=http://localhost:2379", "endpoint", "health"]
      interval: 5s
      timeout: 3s
      retries: 30
      start_period: 10s

  bench:
    image: t4bench
    profiles: [bench]
    networks: [bench]

networks:
  bench:

volumes:
  t41-data:
  t42-data:
  t43-data:
  etcd1-data:
  etcd2-data:
  etcd3-data:
  minio-data:
